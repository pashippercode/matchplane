# This file contains no database URL or password. The service always connects as
# the postgres OS/database user to matchplane through /run/postgresql using peer
# authentication and --no-password.
MATCHPLANE_POSTGRES_BACKUP_RETENTION_DAYS=14
