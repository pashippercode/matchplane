# Configure credentials before enabling MatchPlane services.
MATCHPLANE_ENVIRONMENT=production
# Replace with a unique UUID for this production node before enabling services.
# Set this to a unique UUID for the deployment before enabling services.
MATCHPLANE_NODE_ID=
MATCHPLANE_GRPC_ADDR=0.0.0.0:50051
# Database and Valkey URLs are intentionally absent from this shared file.
# Put each workload's least-privilege URL in /etc/matchplane/services/*.env.
MATCHPLANE_KAFKA_BROKERS=127.0.0.1:9092
MATCHPLANE_KAFKA_SECURITY_PROTOCOL=SSL
MATCHPLANE_KAFKA_SSL_CA_LOCATION=/etc/matchplane/kafka/ca.crt
MATCHPLANE_KAFKA_SSL_CERTIFICATE_LOCATION=/etc/matchplane/kafka/client.crt
MATCHPLANE_KAFKA_SSL_KEY_LOCATION=/etc/matchplane/kafka/client.key
# MATCHPLANE_VALKEY_URL is workload-specific; see /etc/matchplane/services/*.env.
# Optional private CA bundle for a Valkey `rediss://` endpoint. Leave empty when the
# certificate chains to the host trust store; never disable certificate verification.
# MATCHPLANE_VALKEY_CA_FILE=/etc/matchplane/valkey/ca.crt
MATCHPLANE_LOG_FILTER=info,matchplane=info
MATCHPLANE_OTLP_ENDPOINT=https://127.0.0.1:4317
MATCHPLANE_REQUIRE_TLS=true
MATCHPLANE_TLS_CERTIFICATE_PATH=/etc/matchplane/tls/server.crt
MATCHPLANE_TLS_PRIVATE_KEY_PATH=/etc/matchplane/tls/server.key
MATCHPLANE_TLS_CLIENT_CA_PATH=/etc/matchplane/tls/client-ca.crt
# Create these as 32-byte raw, 64-character hex, or base64 keys with mode 0640 root:matchplane.
MATCHPLANE_CONTACT_DATA_KEY_FILE=/etc/matchplane/secrets/gateway/contact-data.key
MATCHPLANE_CONTACT_DATA_KEY_VERSION=1
MATCHPLANE_INVOICE_DATA_KEY_FILE=/etc/matchplane/secrets/payment/invoice-data.key
MATCHPLANE_INVOICE_DATA_KEY_VERSION=1
# Use a random administrator bearer token of at least 24 bytes.
MATCHPLANE_PAYMENT_ADMIN_TOKEN_FILE=/etc/matchplane/secrets/payment/payment-admin.token
MATCHPLANE_GATEWAY_ADMIN_TOKEN_FILE=/etc/matchplane/secrets/gateway/gateway-admin.token
# Platform-owned HTTPS origin used for payment provider return/notification URLs.
# Leave empty until the operator supplies the actual public HTTPS origin.
MATCHPLANE_PAYMENT_CALLBACK_ORIGIN=
# Root tenant used to scope recursive platform-chat delegation. Set this to the
# deployment's tenant UUID; leaving it empty disables cross-platform fan-out.
MATCHPLANE_ROOT_TENANT_ID=
# Immutable static plugin artifacts staged by the isolated builder.
MATCHPLANE_SUBPLATFORM_ARTIFACT_ROOT=/var/lib/matchplane/subplatform-artifacts
# Writable upload staging root. The isolated builder consumes these opaque archives.
MATCHPLANE_SUBPLATFORM_UPLOAD_ROOT=/var/lib/matchplane/subplatform-uploads
MATCHPLANE_MEDIA_MAX_BYTES=26214400
MATCHPLANE_HOSTED_MEDIA_ROOT=/var/lib/matchplane/media
# The isolated builder runs as its own service. This shared value is the builder-side path; the
# web unit overrides it to /etc/matchplane/secrets/web/builder.token so the two Unix users retain
# separate readable copies of the same callback credential.
MATCHPLANE_SUBPLATFORM_BUILDER_WEB_URL=http://127.0.0.1:4173
MATCHPLANE_SUBPLATFORM_BUILDER_TOKEN_FILE=/etc/matchplane/secrets/builder/builder.token
MATCHPLANE_SUBPLATFORM_BUILDER_WORK_ROOT=/var/lib/matchplane/subplatform-builder-work
MATCHPLANE_SUBPLATFORM_BUILDER_BUILD_SECONDS=900
# Optional operator-selected Bun executable. Leave empty to resolve `bun` from the builder
# service PATH; set an absolute path such as /opt/bun/bin/bun when using the latest official
# Bun installer without pinning the subplatform package to a runtime version.
MATCHPLANE_SUBPLATFORM_BUILDER_BUN=
MATCHPLANE_SUBPLATFORM_GIT_ALLOWED_HOSTS=github.com,gitlab.com
# Read-only deployment-issued child SMTP secret slots. References stored by child admins are
# opaque secret://subplatform/<tenant>/<domain>/<name> values, never env:// or file:// paths.
MATCHPLANE_SUBPLATFORM_SECRET_ROOT=/etc/matchplane/secrets/subplatform-email
# Optional organization UUID for a root-scoped machine API key. Without it,
# machine keys are limited to their child platform path and cannot submit at '/'.
MATCHPLANE_ROOT_PLATFORM_ORGANIZATION_ID=
# Optional server-side Agent router. The platform pays token costs. Put
# MATCHPLANE_ROUTER_AI_KEY only in /etc/matchplane/secrets/web/router-ai.env (0640 root:matchplane-web),
# never in this shared environment file or in a browser-visible variable.
MATCHPLANE_ROUTER_AI_URL=
MATCHPLANE_ROUTER_AI_MODEL=
# Wire protocol at the server-side model boundary: openai-compatible (default),
# anthropic-messages, or gemini-generate-content. Never put a provider key in the URL.
MATCHPLANE_ROUTER_AI_PROTOCOL=openai-compatible
MATCHPLANE_ROUTER_AI_MAX_TOKENS=512
MATCHPLANE_ROUTER_AI_TOOL_MODE=auto
MATCHPLANE_ROUTER_AI_MAX_STEPS=8
MATCHPLANE_ROUTER_AI_MAX_FANOUT=4
# Total wall-clock budget for one recursive routing request (4s per hop remains the provider cap).
MATCHPLANE_ROUTER_AI_TOTAL_TIMEOUT_MS=20000
MATCHPLANE_ROUTER_AI_REQUESTS_PER_HOUR=120
MATCHPLANE_ROUTER_AI_GLOBAL_REQUESTS_PER_HOUR=120
# Next standalone web service / Better Auth. Set BETTER_AUTH_SECRET only in
# /etc/matchplane/secrets/web/better-auth.env (0640 root:matchplane-web), never in this shared file.
# Generate it with `openssl rand -base64 32`. Set the public HTTPS origin below; Better Auth
# validates this exact origin, so do not copy a domain from another installation.
BETTER_AUTH_URL=
# Root OIDC issuer for subplatforms hosted on a different origin. Dynamic client
# registration stays disabled; operators register exact redirect URIs through the
# root administrator API.
MATCHPLANE_OIDC_ENABLED=true
# Optional comma-separated origins for explicitly configured additional front ends.
BETTER_AUTH_TRUSTED_ORIGINS=
# Set the root superadmin email before the first account is created. The package
# intentionally leaves this unset; the operator must provide an owned address.
MATCHPLANE_ROOT_ADMIN_EMAIL=
# Optional national network identity provider. Keep the client secret in the web
# secret file/manager; set endpoint values only after official application access
# or an approved SDK gateway has been issued.
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_CLIENT_ID=
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_DISCOVERY_URL=
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_AUTHORIZATION_URL=
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_TOKEN_URL=
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_USERINFO_URL=
# MATCHPLANE_NATIONAL_IDENTITY_OAUTH_SCOPES=openid
# Contact exchange uses only Better Auth email/phone bindings verified by the root account.
# Packages and operators cannot define manual contact-entry fields.
# Root SMTP is deployment-owned because it is needed before the first administrator can log in.
# Keep the credential out of this file; use a secret reference such as
# file:///etc/matchplane/secrets/web/root-smtp-password or env://MATCHPLANE_ROOT_SMTP_PASSWORD.
# MATCHPLANE_ROOT_SMTP_PROVIDER_KEY=root-smtp
# MATCHPLANE_ROOT_SMTP_HOST=smtp.example.com
# MATCHPLANE_ROOT_SMTP_PORT=587
# MATCHPLANE_ROOT_SMTP_TLS_MODE=starttls
# MATCHPLANE_ROOT_SMTP_USERNAME=no-reply@example.com
# MATCHPLANE_ROOT_SMTP_CREDENTIAL_SECRET_REF=file:///etc/matchplane/secrets/web/root-smtp-password
# MATCHPLANE_ROOT_SMTP_FROM_ADDRESS=no-reply@example.com
# MATCHPLANE_ROOT_SMTP_REPLY_TO=
# MATCHPLANE_ROOT_SMTP_MODE=production
# MATCHPLANE_ROOT_SMTP_ENABLED=true
MATCHPLANE_GATEWAY_INTERNAL_URL=http://127.0.0.1:8080
MATCHPLANE_PAYMENT_INTERNAL_URL=http://127.0.0.1:8081
